class Solution:
    def getSum(self, a: int, b: int) -> int:
        """
        4+7
        100
        111
       1011 => 11

        5 = 0101
3 = 0011
```

### **2단계: XOR (자리올림 무시)**
```
  0101
^ 0011
------
  0110  = 6
```
→ 각 자리에서 1+0=1, 0+1=1, 1+1=0(자리올림 발생하지만 무시)

### **3단계: AND + 왼쪽 시프트 (자리올림)**
```
  0101
& 0011
------
  0001  (어디서 자리올림이 발생했는지)

0001 << 1 = 0010  = 2
```
→ 두 비트가 모두 1인 곳에서 자리올림 발생

### **4단계: 반복 (6 + 2)**
```
  0110
^ 0010
------
  0100  = 4  (XOR)

  0110
& 0010
------
  0010
<< 1
------
  0100  = 4  (Carry)
```

### **5단계: 반복 (4 + 4)**
```
  0100
^ 0100
------
  0000  = 0  (XOR)

  0100
& 0100
------
  0100
<< 1
------
  1000  = 8  (Carry)
```

### **6단계: 반복 (0 + 8)**
```
Carry가 0이 될 때까지 반복
0 + 8 = 8  ✅

-1 => 1
1

        """
        mask = 0xFFFFFFFFF # Python은 integer가 32-bit로 고정되어 있지 않으므로, 32bit만 쓰도록 masking 필요.
        max_int = 0x7FFFFFFF
        while b != 0: # carry 없을 때까지 덧셈.
            sum_without_carry = (a ^ b) & mask

            carry = (a & b) << 1 # a, b 가 모두 1인 자리는 다음으로 carry 발생.

            a = sum_without_carry
            b = carry
        
        return a if a <= max_int else ~(a ^ mask)